app.module('ksn', ["ngRoute"]);

app.config(function($routeProvider) {
    $routeProvider
     .when('/druguse', {
        templateUrl: 'views/druguse/druguse.html', 
       // controller: 'PersonAge1'
      })
		
		.otherwise( { redirectTo: '/' } );
});